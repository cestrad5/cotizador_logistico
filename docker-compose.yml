services:
  cotizador:
    build: .
    container_name: cotizador-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
